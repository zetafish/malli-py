# malli-py

A Python port of [Clojure's malli](https://github.com/metosin/malli) — data-driven schemas for validation, coercion, and generation.

## Install

```bash
pip install -e ".[dev]"
```

## Test

```bash
pytest
```
